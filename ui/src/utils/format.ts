// 文件职责：通用格式化工具函数（时间、文件大小、状态文本等）。
// 边界：仅包含纯函数工具；不依赖 Vue 响应式 API 或外部状态。

// TODO(M3)：实现 formatFileSize，将字节数转为人类可读格式（KB/MB/GB）。
// TODO(M3)：实现 formatDateTime，将 ISO 时间字符串转为本地化显示。
// TODO(M3)：实现 formatParseStatus，将 parse_status 枚举值转为中文标签和对应颜色。
