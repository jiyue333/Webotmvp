# 文件职责：维护前端环境变量示例，约束运行时配置键名。
# 边界：只维护配置示例键值；上游供本地复制，下游由运行时读取，不保存敏感凭据。
# TODO：
# - [arch][P1][todo] 完成条件：形成可执行的分层契约并消除职责重叠；验证方式：执行 `cd ui && npm run build` 并通过页面基础联调；归属模块：`ui/.env.example`。

VITE_API_BASE_URL=/api/v1
