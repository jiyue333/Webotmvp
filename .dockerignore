# 文件职责：定义 Docker 构建上下文的忽略规则，排除不需要进入镜像的文件，减小构建上下文体积并避免泄露敏感信息。
# 边界：仅控制 docker build 的文件过滤；不影响 Git 追踪或运行时行为。

# TODO(M1)：补充忽略规则。包括 .git/ / .venv/ / __pycache__/ / node_modules/ / .idea/ / .vscode/ / .DS_Store / logs/ / tmp/ / .env / .env.* / !.env.example / docker-compose*.yml / *.log / *.pyc。
