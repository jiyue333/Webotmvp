# 文件职责：定义生产环境 Docker Compose 服务编排，包含应用服务（app/ui）和全部依赖服务（postgres/redis/neo4j/minio），通过 `docker compose up -d` 一键启动完整环境。对齐 mvp.md §5.2。
# 边界：仅定义服务声明、端口映射、环境变量和卷挂载；不包含业务逻辑；运行时配置从 .env 文件读取。

# TODO(M1)：定义 app 服务。build context=. dockerfile=Dockerfile，环境变量引用 .env（DATABASE_URL/REDIS_URL/JWT_SECRET_KEY），端口 8000:8000，depends_on postgres + redis。
# TODO(M1)：定义 ui 服务。build context=./ui，端口 5173:80，depends_on app。
# TODO(M1)：定义 postgres 服务。镜像 paradedb/paradedb:latest（含 pgvector + pg_search），环境变量 POSTGRES_USER/PASSWORD/DB，端口 5432:5432，持久化卷 postgres_data。
# TODO(M1)：定义 redis 服务。镜像 redis:7-alpine，端口 6379:6379，持久化卷 redis_data。
# TODO(M7)：定义 neo4j 服务。镜像 neo4j:5，环境变量 NEO4J_AUTH，端口 7474:7474 + 7687:7687，持久化卷 neo4j_data。
# TODO(M5)：定义 minio 服务（可选）。镜像 minio/minio:latest，环境变量 MINIO_ROOT_USER/PASSWORD，端口 9000:9000 + 9001:9001，持久化卷 minio_data。
# TODO(M1)：定义 volumes 段。声明 postgres_data / redis_data / neo4j_data / minio_data。


