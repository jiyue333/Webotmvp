# 文件职责：Alembic 迁移工具的主配置文件，定义迁移脚本路径、日志格式、数据库连接方式等运行参数。
# 边界：仅声明 Alembic 运行时配置；数据库连接 URL 由 env.py 从 AppSettings 动态注入，不在此文件硬编码。上游由 `alembic` CLI 读取，下游驱动 env.py 执行迁移。

# TODO(M1)：补充 [alembic] 段。设置 script_location = migrations，sqlalchemy.url 留空（由 env.py 动态注入）。
# TODO(M1)：补充 [loggers] / [handlers] / [formatters] 标准日志段。logger_root WARN、logger_sqlalchemy WARN、logger_alembic INFO、handler_console StreamHandler、formatter_generic 含 levelname + name + message。
