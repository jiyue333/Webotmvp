"""
文件职责：维护事件定义与事件总线入口，解耦跨模块通信。
边界：只定义事件与发布订阅契约；上游由业务模块触发，下游由订阅方处理，不耦合具体业务细节。
TODO：
- [arch][P1][todo] 完成条件：形成可执行的分层契约并消除职责重叠；验证方式：执行 `cd src && python -m pytest -q` 并通过相关模块用例；归属模块：`src/app/event/__init__.py`。
"""

