# 文件职责：提供环境变量的键名与默认值示例，供开发者复制为 .env 后按需修改。对齐 mvp.md §3.4 技术栈所需的连接配置。
# 边界：仅作为参考模板；不被运行时直接加载（.env 才是）；不包含真实密钥或凭据。

# TODO(M1)：补充数据库连接。DATABASE_URL=postgresql+asyncpg://webot:webot@localhost:5432/webot
# TODO(M1)：补充 Redis 连接。REDIS_URL=redis://localhost:6379/0
# TODO(M2)：补充 JWT 配置。JWT_SECRET_KEY=change-me-in-production，JWT_ACCESS_EXPIRE_MINUTES=30，JWT_REFRESH_EXPIRE_DAYS=7
# TODO(M7)：补充 Neo4j 配置。NEO4J_ENABLE=false，NEO4J_URI=bolt://localhost:7687，NEO4J_USER=neo4j，NEO4J_PASSWORD=password
# TODO(M5)：补充存储配置。STORAGE_BACKEND=local，MINIO_ENDPOINT=localhost:9000，MINIO_ACCESS_KEY=minioadmin，MINIO_SECRET_KEY=minioadmin
# TODO(M1)：补充应用配置。APP_ENV=development，LOG_LEVEL=INFO，APP_HOST=0.0.0.0，APP_PORT=8000
