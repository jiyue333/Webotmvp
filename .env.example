# 文件职责：维护 `.env.example` 的本地环境变量契约示例。
# 边界：仅提供 MVP 默认开发变量，不承载生产环境安全参数与密钥管理方案。
# TODO：
# - [arch][P1][todo] 在 M1 固化环境变量命名与默认值语义并同步 README。

# App
APP_NAME=webot-mvp
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000

# Database
DATABASE_URL=postgresql+psycopg://webot:webot@localhost:5432/webot

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT
JWT_SECRET_KEY=change-me-in-development
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Neo4j
NEO4J_ENABLE=false
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# Object Storage
STORAGE_BACKEND=local
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=webot

# Frontend
VITE_API_BASE_URL=/api/v1
