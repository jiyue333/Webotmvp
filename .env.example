# 文件职责：维护后端环境变量示例，约束本地开发配置键名与默认值语义。
# 边界：只维护配置示例键值；上游供本地复制，下游由运行时读取，不保存敏感凭据。
# TODO：
# - [ops][P2][todo] 完成条件：补齐运行脚本与部署配置检查项；验证方式：执行对应模块回归检查并通过；归属模块：`.env.example`。

APP_NAME=webot-mvp
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8000

# Database
DATABASE_URL=postgresql+psycopg://webot:webot@localhost:5432/webot

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT
JWT_SECRET_KEY=change-me-in-development
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Neo4j
NEO4J_ENABLE=false
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# Object Storage
STORAGE_BACKEND=local
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=webot

# Frontend
VITE_API_BASE_URL=/api/v1
