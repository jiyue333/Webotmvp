# 文件职责：维护忽略规则，控制构建上下文与仓库追踪范围。
# 边界：只维护忽略规则；上游供 git/docker 读取，下游不影响运行时业务行为。
# TODO：
# - [arch][P1][todo] 完成条件：形成可执行的分层契约并消除职责重叠；验证方式：执行对应模块回归检查并通过；归属模块：`.gitignore`。

.env
# 但不忽略示例文件
!.env.example
*.pem
*_key
*_secret
*.key
*.crt

# IDE和编辑器文件
.idea/
.vscode/
*.swp
*.swo

# 构建和依赖文件
node_modules/
/dist/
/build/
*.log
.trellis/

# 临时文件
tmp/
temp/
logs/
*.pid

WeKnora
/models/
test/data/mswag.txt
data/files/

.venv/
**/__pycache__
.python-version

### macOS
# General
.DS_Store
/scripts/scale_dev_jobs.sh
server
ui/.vite
.ace-tool/
design.pen
