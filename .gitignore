# 文件职责：维护忽略规则，控制构建上下文与仓库追踪范围。
# 边界：只维护忽略规则；上游供 git/docker 读取，下游不影响运行时业务行为。
# TODO(M1)：补充忽略规则。包括：
#   敏感文件：.env / !.env.example / *.pem / *_key / *_secret / *.key / *.crt
#   IDE：.idea/ / .vscode/ / *.swp / *.swo
#   Python：.venv/ / __pycache__/ / *.pyc / *.pyo / *.egg-info/ / dist/ / build/
#   Node：node_modules/ / ui/.vite
#   运行时产物：logs/ / tmp/ / *.log / *.pid
#   系统文件：.DS_Store / Thumbs.db
#   项目特定：data/files/ / .ace-tool/ / design.pen

.env
# 但不忽略示例文件
!.env.example
*.pem
*_key
*_secret
*.key
*.crt

# IDE和编辑器文件
.idea/
.vscode/
*.swp
*.swo

# 构建和依赖文件
node_modules/
/dist/
/build/
*.log
.trellis/

# 临时文件
tmp/
temp/
logs/
*.pid

WeKnora
/models/
test/data/mswag.txt
data/files/

.venv/
**/__pycache__
.python-version

### macOS
# General
.DS_Store
/scripts/scale_dev_jobs.sh
server
ui/.vite
.ace-tool/
design.pen
